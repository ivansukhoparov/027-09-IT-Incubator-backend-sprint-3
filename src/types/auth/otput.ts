export type AuthOutputType = {
    accessToken: string
    refreshToken: string
};

export type AuthAccessTokenType = {
    accessToken: string
};

export type AuthRefreshTokenType = {
    refreshToken: string
};
